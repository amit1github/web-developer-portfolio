// @flow strict

import { skillsData } from "@/utils/data/skills";
import { skillsImage } from "@/utils/skill-image";
import Image from "next/image";
import Marquee from "react-fast-marquee";

function Skills() {
  return (
    <div id="skills" className="relative z-50 border-t my-12 lg:my-24 border-white/10">
      <div className="w-[100px] h-[100px] bg-cyan-500 rounded-full absolute top-6 left-[42%] translate-x-1/2 filter blur-3xl opacity-20"></div>

      <div className="flex justify-center -translate-y-[1px]">
        <div className="w-3/4">
          <div className="h-[1px] bg-gradient-to-r from-transparent via-cyan-500 to-transparent w-full" />
        </div>
      </div>

      <div className="flex justify-center my-5 lg:py-8">
        <div className="flex items-center">
          <span className="w-24 h-[1px] bg-gradient-to-r from-transparent to-cyan-500"></span>
          <span className="bg-slate-900/60 backdrop-blur-md border border-white/10 w-fit text-slate-200 p-2 px-5 text-xl rounded-md shadow-[0_0_20px_rgba(6,182,212,0.1)] uppercase tracking-widest font-semibold flex items-center justify-center">
            Skills
          </span>
          <span className="w-24 h-[1px] bg-gradient-to-l from-transparent to-cyan-500"></span>
        </div>
      </div>

      <div className="w-full my-12">
        <Marquee
          gradient={false}
          speed={80}
          pauseOnHover={true}
          pauseOnClick={true}
          delay={0}
          play={true}
          direction="left"
        >
          {skillsData.map((skill, id) => (
            <div className="w-36 min-w-fit h-fit flex flex-col items-center justify-center transition-all duration-500 m-3 sm:m-5 rounded-lg group relative hover:scale-[1.15] cursor-pointer"
              key={id}>
              <div className="h-full w-full rounded-xl border border-white/10 bg-slate-900/40 backdrop-blur-sm shadow-[0_0_20px_rgba(6,182,212,0.05)] group-hover:border-cyan-400/50 group-hover:shadow-[0_0_30px_rgba(6,182,212,0.2)] transition-all duration-500">
                <div className="flex -translate-y-[1px] justify-center">
                  <div className="w-3/4">
                    <div className="h-[1px] w-full bg-gradient-to-r from-transparent via-cyan-500 to-transparent" />
                  </div>
                </div>
                <div className="flex flex-col items-center justify-center gap-3 p-6">
                  <div className="h-8 sm:h-10">
                    <Image
                      src={skillsImage(skill) as string}
                      alt={skill}
                      width={40}
                      height={40}
                      className="h-full w-auto rounded-lg"
                    />
                  </div>
                  <p className="text-slate-300 font-medium text-sm sm:text-lg group-hover:text-cyan-400 transition-colors duration-300">
                    {skill}
                  </p>
                </div>
              </div>
            </div>
          ))}
        </Marquee>
      </div>
    </div>
  );
};

export default Skills;